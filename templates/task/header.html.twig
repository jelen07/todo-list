<nav class="navbar navbar-expand-lg navbar-dark bg-dark" aria-label="Tenth navbar example">
    <div class="container" style="max-width: 40rem;">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link active navbar-brand" aria-current="page" href="{{ path('app_tasks') }}">📋 Simple Todo List</a>
            </li>
        </ul>
        <ul class="navbar-nav d-inline-flex mt-2 mt-md-0 ms-md-auto">
            <li class="nav-item dropdown">
                {% if app.user %}
                    <a class="nav-link dropdown-toggle" href="#" id="dropdown08" data-bs-toggle="dropdown" aria-expanded="false" title="{{ app.user.email }}">👤 {{ app.user.name }}</a>
                    <ul class="dropdown-menu" aria-labelledby="dropdown08">
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">🏃 Log Out</a></li>
                    </ul>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>
